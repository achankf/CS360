True.
\begin{itemize}
\item
Suppose $\epsilon \in L$.
Then $L = \{\epsilon\} L \subseteq LL$.

\item
Suppose $\epsilon \not\in L$.
Let $min(L)$ be the size of the smallest string in $L$.
Since $s$ is non-empty, $min(L) \ge 1$.
However, $L \not\subseteq LL$ because $min(LL) \ge 2min(L) \ge min(L) + 1 > min(L)$.
In other words, the shortest string in $L$ is not in $LL$.
\end{itemize}
Therefore, the statement is true.
\done
